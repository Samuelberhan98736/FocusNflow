import 'package:flutter/material.dart';

class GroupDetailsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Group Details")),
      body: Padding(
        padding: EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text("Group name: The Math Masters"),
            Text("Group ID: 0v63bc68a"),
            Text("Recent Messages: Wong60: I'm here"),
            Text("Member Count: 7/20"),
            Text("Current session timer: 35 minutes"),
            SizedBox(height: 20),
            ElevatedButton(
              child: Text("CHAT"),
              onPressed: () => Navigator.pushNamed(context, '/groupchat'),
            )
          ],
        ),
      ),
    );
  }
}
